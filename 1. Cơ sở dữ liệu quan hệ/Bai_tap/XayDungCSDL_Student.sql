create database Student;

create table Student.Class (
   id int,
   name varchar(30)
   
);

create table Student.Teacher (
     id int,
     name varchar(30),
     age int,
     country varchar(30)
);